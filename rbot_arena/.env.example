# Arena Gateway
ARENA_HOST=0.0.0.0
ARENA_PORT=8787
PIN_CODE=841921

# JWT Auth
JWT_SECRET=change_me_very_secret_production_key_here
JWT_REFRESH_SECRET=change_me_refresh_secret_production_key_here
JWT_EXPIRE_MIN=30
JWT_REFRESH_EXPIRE_DAYS=14

# Seed one admin (optional quick start)
SEED_ADMIN_EMAIL=admin@local
SEED_ADMIN_PASSWORD=changeme_admin

# Guardrails
OCO_REQUIRED=true
QUALITY_THRESHOLD=70
MAX_RISK_PCT=1.0
MAX_HOLD_MIN=360
DAILY_HALT_PCT=-5.0

# Execution switches (kept OFF until you're ready)
EXECUTION_ENABLED=false
PAPER_MODE=true

# LLM routing
LLM_PROVIDER=rick            # rick | openai | router
RICK_BASE_URL=http://127.0.0.1:8000/v1
RICK_MODEL=rick-13b-instruct
OPENAI_API_KEY=
HIVE_ROUTE=gpt,grok,deepseek,github     # only used if LLM_PROVIDER=router

# OpenAlgo bridge
OPENALGO_HOST=http://127.0.0.1:5000
OPENALGO_API_KEY=
OPENALGO_WS=ws://127.0.0.1:8765
ZMQ_HOST=127.0.0.1
ZMQ_PORT=5555

# Market Data API (existing service)
MARKET_DATA_API=http://127.0.0.1:5560
MARKET_DATA_SERVICE_TOKEN=
